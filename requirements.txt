# DataLens AI - dependencies
# Tested against Python 3.11 (recommended). Also compatible with Python 3.12.
# If you use a different Python version and hit an install error, remove the
# version pins below and let pip resolve compatible versions instead.

streamlit==1.40.1
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1
python-dotenv==1.0.1
google-genai==0.8.0
pytest==8.3.3
